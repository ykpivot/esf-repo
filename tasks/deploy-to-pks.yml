---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu

inputs:
  - name: esf-repo

outputs:
  - name: config-files

run:
  path: sh
  args:
  - -exc
  - |
    env
    echo "Run command to push to PKS"
    echo "Run command to start the cluster"